<Window x:Class="ProductViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Product Viewer" Height="450" Width="600"
        Background="#1E1E2E">

    <Window.Resources>
        <Style x:Key="HeaderText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style x:Key="TitleText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#89B4FA"/>
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="🛍 Products" Style="{StaticResource TitleText}"/>

        <!-- Column Headers -->
        <Grid Grid.Row="1" Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="120"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="ID" Style="{StaticResource HeaderText}"/>
            <TextBlock Grid.Column="1" Text="Name" Style="{StaticResource HeaderText}"/>
            <TextBlock Grid.Column="2" Text="Price (USD)" Style="{StaticResource HeaderText}" HorizontalAlignment="Right"/>
        </Grid>

        <Separator Grid.Row="1" VerticalAlignment="Bottom" Background="#45475A" Margin="0,24,0,0"/>

        <!-- Product List -->
        <ListView x:Name="ProductList" Grid.Row="2" Margin="0,8,0,0"
                  Background="Transparent" BorderThickness="0"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Padding" Value="0,6"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#313244"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#45475A"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="120"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Id}" Foreground="#6C7086" FontSize="13"/>
                        <TextBlock Grid.Column="1" Text="{Binding ProductName}" Foreground="#CDD6F4" FontSize="13"/>
                        <TextBlock Grid.Column="2" Text="{Binding Price, StringFormat='${0:N2}'}"
                                   Foreground="#A6E3A1" FontSize="13" HorizontalAlignment="Right"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Footer: total -->
        <Border Grid.Row="3" Background="#313244" CornerRadius="6" Padding="12,8" Margin="0,12,0,0">
            <Grid>
                <TextBlock Text="Total" Foreground="#BAC2DE" FontSize="13"/>
                <TextBlock x:Name="TotalText" HorizontalAlignment="Right"
                           Foreground="#F9E2AF" FontWeight="Bold" FontSize="13"/>
            </Grid>
        </Border>
    </Grid>
</Window>
