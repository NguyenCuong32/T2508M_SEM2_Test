<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | TreeShop</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <header class="site-header">
    <div class="nav-shell">
      <a class="brand" href="/treeshop">Tree Shop</a>
      <nav class="main-nav" aria-label="Main navigation">
        <a class="<%= currentPath === '/treeshop' || currentPath === '/' || currentPath.startsWith('/trees') ? 'active' : '' %>" href="/treeshop">TreeShop</a>
        <a class="<%= currentPath === '/about' ? 'active' : '' %>" href="/about">About me</a>
      </nav>
    </div>
  </header>
