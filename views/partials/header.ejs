<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tree Shop</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    <!-- Question 2: Header with include - 2 pages: TreeShop and About me -->
    <header class="navbar">
        <nav>
            <a href="/" class="nav-link <%= typeof currentRoute !== 'undefined' && currentRoute === '/' ? 'active' : '' %>">Tree Shop</a>
            <a href="/about" class="nav-link <%= typeof currentRoute !== 'undefined' && currentRoute === '/about' ? 'active' : '' %>">About me</a>
        </nav>
    </header>